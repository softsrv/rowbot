---
name: design-skill
description: Use when creating, redesigning, or restyling any page/template in this repo (web/templates/) — new UI for a feature, or a request like "this page looks ugly/cluttered/inconsistent." Documents RowBot's established daisyUI/Tailwind visual conventions (card+icon+badge row pattern, page structure, icon/badge/modal rules, CSP-safe JS wiring) and the no-browser-tooling screenshot-verification workflow used to catch visual bugs before calling a template change done.
---

Full guide: `docs/design-conventions.md` at the repo root. Read it in full
before making any UI/template change — it's tool-agnostic (not Claude-specific)
so it's also linked from `AGENTS.md` for other coding agents working in this
repo.
